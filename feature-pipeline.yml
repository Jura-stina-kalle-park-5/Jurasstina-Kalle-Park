trigger:
- feature/*

pool:
  vmImage: 'ubuntu-latest'

steps:

  - template: templates/install-dependencies.yaml

  - script: |
      robot --variable BROWSER:headlesschrome --xunit results/logs/output_feature_test_xunit.xml --include new-feature --output results/logs/output_feature_test.xml --log results/logs/log_feature_test.html --report results/logs/report_feature_test.html .
    displayName: 'Run Tests tagged with new-feature'

  # Publish the Robot Framework reports as pipeline artifacts.
  - task: PublishPipelineArtifact@1
    inputs:
      targetPath: '$(System.DefaultWorkingDirectory)/results/logs/'
      artifactName: 'robot-reports-feature'
    displayName: 'Publish Robot Framework Reports'

  - task: PublishTestResults@2
    inputs:
      testResultsFormat: 'XUnit' # 'JUnit' | 'NUnit' | 'VSTest' | 'XUnit' | 'CTest'. Alias: testRunner. Required. Test result format. Default: JUnit.
      testResultsFiles: 'results/logs/output_feature_test_xunit.xml'
    displayName: 'Publish Robot Framework Test Results'


